# DeepSeek API Key（从 https://platform.deepseek.com 申请）
DEEPSEEK_API_KEY=sk-your-api-key-here

# JWT 密钥（auth.py 用到，可以用 `python -c "import secrets; print(secrets.token_hex(32))"` 生成）
# JWT_SECRET_KEY=your-random-secret-here
