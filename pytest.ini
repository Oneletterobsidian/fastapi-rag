[pytest]
# 让pytest把项目根目录（这个文件所在的目录）加入Python的模块搜索路径
# 这样tests/里的测试文件才能正常 `import main`、`from doc_utils import ...`
# 不需要依赖你是用 `pytest` 还是 `python -m pytest` 来运行，两种方式都能正常工作
pythonpath = .
testpaths = tests
